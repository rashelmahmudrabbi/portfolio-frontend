// Point this at your deployed backend (Node/Express on Vercel, see
// portfolio-backend/README.md for the full deploy steps).
//
// Replace <your-project> below with your actual Vercel project name once
// deployed — e.g. https://portfolio-backend.vercel.app/api
const API_BASE = 'https://<your-project>.vercel.app/api';

// Local dev, running `npm run dev` in the backend folder:
//   const API_BASE = 'http://localhost:8000/api';